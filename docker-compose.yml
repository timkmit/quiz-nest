version: '3.10'

services:
  app:
    build: .
    container_name: nest_app
    environment:
      - NODE_ENV=production
    restart: always

  nginx:
    depends_on:
      - app
    image: nginx
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d